export enum resources {
  base = '/',
  blogs = 'blogs',
  posts = 'posts',
  users = 'users',
  auth = 'auth',
  comments = 'comments',
}

export enum dbCollections {
  blogs = 'blogs',
  posts = 'posts',
  users = 'users',
}
